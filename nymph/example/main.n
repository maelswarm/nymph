#include <stdlib.h>
#include <stdio.h>
#include "box.n"
#include "rect.n"

private int main(int argc, const char * argv[]) {

    Box *myBox = new Box;    
    Rect *myRect = new Rect;

    printBox(myBox);
    printRect(myRect);

    free(myBox);
    free(myRect);

    return 0;
}
