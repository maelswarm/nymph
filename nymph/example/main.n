#include <stdlib.h>
#include "box.n"
#include "rect.n"

int add(int a, int b) {
    return a+b;
}

int main(int argc, const char * argv[]) {
    Box **myBoxes = new Box*10;
    Box *myBox;
    Rect *myRect;
    myBox=new Box;
    myRect=new Rect;
    myBoxes[0]=myBox;

    volatile int *a = new int;

    printBox(myBoxes[0]);
    printRect(myRect);

    free(myBox);
    free(myBoxes);
    free(myRect);

    return 0;
}
