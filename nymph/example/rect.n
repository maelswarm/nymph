#include <stdlib.h>
#include <stdio.h>
#include "main.n"

pub obj Rect {
    int height = 1;
    int width = 1;
}

pub void printRect(Rect *this) {
    printf("%i %i\n", this->height, this->width);
}
