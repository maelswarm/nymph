#include <stdlib.h>
#include <stdio.h>
#include "rect.n"

int add(int a, int b);

pub obj Box:Rect {
  int width = 20;
  int height = 1;
  int depth = 1;
}

pub void print(Box *this) {
    printf("%i\n", add(this->width, this->height));
}

int add(int a, int b) {
  return a + b;
}
