#include <stdlib.h>
#include <stdio.h>

pub obj Box {
  int width = 1;
  int height = 1;
  int depth = 1;
}

int add(int a, int b);

pub void printBox(Box *this) {
    printf("%i\n", add(this->width, this->height));
}

int add(int a, int b) {
  return a + b;
}
